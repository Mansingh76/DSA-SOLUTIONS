136. Single Number
XOR all numbers → duplicates cancel out → only single number remains.

class Solution {
    public int singleNumber(int[] nums) {
        int result = 0;
        for (int num : nums) result ^= num;
        return result;
    }
}
